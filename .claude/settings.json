{
  "permissions": {
    "allow": [
      "mcp__context7__resolve-library-id",
      "mcp__context7__get-library-docs",
      "Bash(mkdir:*)",
      "Bash(npm run build:*)",
      "Bash(npm run typecheck:*)"
    ],
    "deny": [],
    "ask": []
  },
  "hooks": {
    "PreToolUse": [],
   "PostToolUse": [
  {
    "matcher": "Edit|Write",
    "hooks": [
      {
        "type": "command",
        "command": "echo \"[After Edit] Running typecheck...\" && if ! npm run typecheck --silent; then echo \"[After Edit] Type errors — Claude must fix.\"; exit 2; fi"
      },
      {
        "type": "command",
        "command": "echo \"[After Edit] Running build...\" && if ! npm run build --silent; then echo \"[After Edit] Build failed — Claude must fix.\"; exit 2; fi"
      }
    ]
  }
  ]
  }
}